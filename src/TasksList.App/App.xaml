<Application x:Class="TasksList.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options">
    <Application.Resources>
        <SolidColorBrush x:Key="CanvasBrush" Color="#202020" po:Freeze="False" />
        <SolidColorBrush x:Key="PanelBrush" Color="#272727" po:Freeze="False" />
        <SolidColorBrush x:Key="CardBrush" Color="#2D2D2D" po:Freeze="False" />
        <SolidColorBrush x:Key="CardHoverBrush" Color="#383838" po:Freeze="False" />
        <SolidColorBrush x:Key="BorderBrush" Color="#454545" po:Freeze="False" />
        <SolidColorBrush x:Key="PrimaryBrush" Color="#0078D4" po:Freeze="False" />
        <SolidColorBrush x:Key="PrimarySoftBrush" Color="#360078D4" po:Freeze="False" />
        <SolidColorBrush x:Key="TextBrush" Color="#FFFFFF" po:Freeze="False" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#C7C7C7" po:Freeze="False" />
        <SolidColorBrush x:Key="SuccessBrush" Color="#6CCB8B" po:Freeze="False" />
        <SolidColorBrush x:Key="DangerBrush" Color="#FF99A4" po:Freeze="False" />
        <SolidColorBrush x:Key="FocusBrush" Color="#0078D4" po:Freeze="False" />
        <SolidColorBrush x:Key="DisabledTextBrush" Color="#858585" po:Freeze="False" />
        <SolidColorBrush x:Key="AccentTextBrush" Color="#FFFFFF" po:Freeze="False" />

        <Style TargetType="Window">
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource CanvasBrush}" />
            <Setter Property="Icon" Value="/TasksList.App;component/Assets/TasksList.ico" />
        </Style>

        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource CardHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.72" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource FocusBrush}" />
                                <Setter TargetName="Root" Property="BorderThickness" Value="2" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource DisabledTextBrush}" />
                                <Setter TargetName="Root" Property="Opacity" Value="0.56" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="Foreground" Value="{StaticResource AccentTextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="15,9" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource CardBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="11,8" />
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryBrush}" />
        </Style>
    </Application.Resources>
</Application>
